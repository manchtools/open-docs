---
title: v0.3.2 — pre-built default docs
date: 2026-06-08
tags: release, docker
---

# v0.3.2 — pre-built default docs

The image bakes the default documentation at image-build time, so a
plain `docker run` serves instantly on a few megabytes instead of
building at start.
